/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package NotePadApp;

/**
 *
 * @author maxknee1
 */
@Override
public interface Comprable {
    Note note = null;
    (ERROR) ;
    .note ;
    Note ;
    (ERROR) ;

    note.tags.equals <error>((ERROR) ) {
        return 1;
    }
    (ERROR) ;

    note.tags.equals <error>((ERROR) ) {
        return -1;
    }
    
}
